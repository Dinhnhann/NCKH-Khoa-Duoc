@model IEnumerable<_2021NCKH.Models.ChiThucVat>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Tên Chi Thực Vật
        </th>
        <th>
            Loại Họ Thực Vật
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenChiThucVat)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.HoThucVat.TenHoThucVat)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MaChiThucVat }) |
            @Html.ActionLink("Details", "Details", new { id=item.MaChiThucVat }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MaChiThucVat })
        </td>
    </tr>
}

</table>
